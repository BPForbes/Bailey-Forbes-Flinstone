/**
 * **P3-11 — IPv6 + ICMPv6 `[DEFERRED]`** (module contract, normative).
 *
 * This row is an **explicit deferral** for dual-stack work, not an accidental omission.
 * Until promoted:
 *   - Normative references for the future bundle: **RFC 8200**, **RFC 4291**, **RFC 4443**,
 *     **RFC 4861** (ND); ties to **P3-1** L2 and **P3-8** **AAAA** when promoted.
 *   - **P3-5** remains the primary Internet path on **develop** until this shard moves to **✅**.
 *
 * See **docs/ROADMAP.md** Phase 3 and standards appendix.
 */
#ifndef FL_CONTRACT_P3_IPV6_DEFERRED_H
#define FL_CONTRACT_P3_IPV6_DEFERRED_H

#include "contract_identity.h"

#define FL_CONTRACT_P3_11_IPV6_DEFERRED_CONTRACT_DEFINED 1

#endif /* FL_CONTRACT_P3_IPV6_DEFERRED_H */
