# Visual Studio
.vs/

# Build artifacts
*.o
BPForbes_Flinstone_Shell
BPForbes_Flinstone_Tests

# Shell history (user-specific)
shell_history.txt
userland/shell/fl_users.db

# Test/generated files
*_disk
*_disk.dat
*_disk.img
drive.img
testfile.txt
tests/test_alloc
tests/test_mem_asm
tests/test_priority_queue
tests/test_drivers
tests/test_vm_mem
tests/test_invariants
tests/test_fs_jail
tests/test_audit_log
tests/test_replay
tests/test_userspace_connection
tests/test_vm_layer_warning
tests/test_vm_syscall_bridge
tests/test_vm_arch_readiness

# Fetched/built deps
deps/install
deps/install-aarch64
deps/build
deps/download

# Local planning (not pushed)
plan/
.git-ops.sh
git-push-log.txt


# VM checkpoint disk copy
vm_checkpoint_disk.img
# Default embedded-VM virtual disk (created at run time)
vm_disk.img
# VM filesystem jail (created under launch cwd or g_vm_root)
vm_hostfs/

# Disk journal temp (crash consistency)
*.tmp

# Generated by CI only (scripts/gen_version_changelog.c); local make omits unless CHANGELOG_CI=1
userland/shell/version_changelog.c
# Built locally or in CI before running the generator (not tracked)
/gen_version_changelog

# Makefile: aggregate checksum of version/entries/**/*.ver for version_def.h freshness
.version_entries_ver.sum
