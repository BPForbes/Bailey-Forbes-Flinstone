P2 (*contracts/identity*) — roadmap **P2-1** … **P2-4** (principal model, credential
store, authorization middleware, sudo-like elevation).

Add headers here as they are authored. Each new header should start with:
  #include "contract_extend.h"
so **P0 foundations** stay the base (see ../foundations/).

When this tree is used, add **-Icontracts/identity** to the relevant Makefile or
CMake target.
