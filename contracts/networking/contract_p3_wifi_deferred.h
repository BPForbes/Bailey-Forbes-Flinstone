/**
 * **P3-10 — Wi‑Fi station path `[DEFERRED]`** (module contract, normative).
 *
 * This row is an **explicit deferral**, not a silent gap. Until promoted:
 *   - Do **not** treat **P3-10** as an **A1–A2** gate; see **docs/ROADMAP.md** Phase 3 table.
 *   - Informative stack references: **IEEE 802.11**, **802.11i**, **802.1X** / **EAP**;
 *     expect **P4**-class firmware/driver work plus reuse of **P3-5** / **P3-6** / **P3-7**
 *     when un-deferred.
 *   - **P3-12** DHCP after L2 still applies as a product ordering constraint when Wi‑Fi ships.
 *
 * See **docs/ROADMAP.md** Phase 3 and credibility notes on multi-track staffing.
 */
#ifndef FL_CONTRACT_P3_WIFI_DEFERRED_H
#define FL_CONTRACT_P3_WIFI_DEFERRED_H

#include "contract_identity.h"

#define FL_CONTRACT_P3_10_WIFI_DEFERRED_CONTRACT_DEFINED 1

#endif /* FL_CONTRACT_P3_WIFI_DEFERRED_H */
