/*
 * Example only — NOT compiled by `make`. Copy or generate a real changelog at
 * deployment; do not require this for local git clone + make.
 *
 * Example deployment output: concatenate per-branch notes or emit from CI.
 */
#if 0
const char VERSION_CHANGELOG[256] =
    "2.2.4 — Example entry\n";
#endif
