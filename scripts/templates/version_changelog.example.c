/*
 * Example only — NOT compiled by `make`. CI generates userland/shell/version_changelog.c
 * from version/entries/*.ver when CHANGELOG_CI=1 (headline version = highest entry).
 */
#if 0
const char VERSION_CHANGELOG[] =
    "2.2.4 — changelog from version entry files\n"
    "2.2.4: Example description (version/entries/2_2_4_baseline.ver)\n";
#endif
